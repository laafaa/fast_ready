<div class="container">
  <div class="col-md-5 col-md-push-3">
    <h2>Регистрация</h2>

    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= f.error_notification %>
      <%= f.input :login, placeholder: 'Login' %>
      <%= f.input :phone, hint: 'Номер телефона должен содержать 11 цифр' %>
      <%= f.input :email, placeholder: 'user@domain.com' %>
      <%= f.input :password, placeholder: '*************', hint: 'Минимум 8 символов' %>
      <p>Нажимаю кнопку регистрация, вы подтверждаете, что ознакомлены с <%= link_to "Регламентом", schedule_path %> и обязуетесь соблюдать данные правила.</p>
      <%= f.button :submit, 'Регистрация' %>
    <% end %>
    <%= render "devise/shared/links" %>
  </div>
</div>