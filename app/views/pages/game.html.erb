<div class="container">
  <% if user_signed_in? %>
  	<div class="row">
  		<% if flash[:zero_balance]%>
  			<div class="alert alert-danger" role="alert">
  				<%= flash[:zero_balance]%>
  			</div>
  		<% end %>
  		<% if flash[:add]%>
  			<div class="alert alert-success" role="alert">
  				<%= flash[:add]%>
  			</div>
  		<% end %>
  		<% if flash[:not_add]%>
  			<div class="alert alert-warning" role="alert">
  				<%= flash[:not_add]%>
  			</div>
  		<% end %>

  		<div class="jumbotron">
    		<h2 class="text-center">Как проходит мгновенная лотерея</h2>
    		<p>Мгновенная лотерея может состоять из 10 человек. После того как набирается 10 человек, розыгрыш начинается автоматически. Система выбирает случайным образом победителя. Отследить выбор победителя можно в онлайн режиме, после выбора победителя, выйгрыш начисляется на аккаунт победителя.</p>
  		</div>

  		<div class="container">
        <ul class="nav nav-tabs" role="tablist">
          <li role="presentation" class="active"><a href="#active" aria-controls="active" role="tab" data-toggle="tab"><span class="" aria-hidden="true"></span> Активные игры </a></li>
          <li role="presentation"><a href="#end" aria-controls="end" role="tab" data-toggle="tab"><span class="" aria-hidden="true"></span> Завершенные игры</a></li>
        </ul>
        <div class="tab-content">
          <div role="tabpanel" class="tab-pane active" id="active">
            <div class="row" id="game_pad">
              <%= render "shader/game_active" %>
            </div>
          </div>
          <div role="tabpanel" class="tab-pane" id="end">
            <div class="row" id="game_pad">
              <%= render "shader/game_end" %>
            </div>
          </div>
        </div>
  	</div>
  <% else %>
		<%= render "shader/unincluded" %>
  <% end %>
  </div>
</div>
